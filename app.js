function greet (firstName = 'Katelyn', lastName = 'Harvey'){
  return 'You are beautiful ' + firstName + ' ' + lastName;
}
console.log(greet());

const phi = function(){
   return (1 + Math.sqrt(5))/2
 }
console.log(phi());